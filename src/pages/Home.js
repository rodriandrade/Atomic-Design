import React from 'react'
import { Blockquote, Form, Inner, Nav } from '../components/Index'

const Home = () => {
    return (
        <div className="App">
            <Inner>
                <Nav />
            </Inner>
        </div>
    )
}

export default Home